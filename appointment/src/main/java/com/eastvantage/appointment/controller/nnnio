<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Excel Config Editor</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .filter-box {
      @apply absolute z-10 bg-white border border-gray-300 shadow p-2 space-y-2;
    }
    input[type="text"], input[type="checkbox"] {
      @apply border border-gray-300 rounded px-2 py-1 w-full;
    }
    table input {
      @apply w-full bg-transparent outline-none;
    }
    thead th {
      @apply bg-gray-100 sticky top-0 z-10 text-sm text-left px-2 py-1;
    }
    tbody td {
      @apply px-2 py-1;
    }
    .enabled {
      @apply bg-blue-600 text-white cursor-pointer;
    }
    .filter-icon {
      @apply ml-2 cursor-pointer text-gray-500 hover:text-blue-500;
    }
    .clear-filters {
      @apply text-sm text-red-500 cursor-pointer float-right;
    }
  </style>
</head>
<body class="bg-gray-50 text-gray-800">
  <!-- Top Navbar -->
  <div class="flex items-center justify-between bg-blue-600 text-white px-4 py-2 shadow sticky top-0 z-50">
    <div class="flex items-center gap-2">
      <button id="toggleDrawer" class="text-2xl font-bold">☰</button>
      <span id="sheetTitle" class="text-lg font-semibold">Excel Config Editor</span>
    </div>
    <div id="toolbar" class="space-x-2 hidden sm:flex">
      <button onclick="addRow()" class="bg-green-600 hover:bg-green-700 px-3 py-1 rounded">＋ Add</button>
      <button id="btnRemove" onclick="removeSelected()" class="px-3 py-1 rounded bg-gray-300 text-gray-600">– Remove</button>
      <button id="btnSave" onclick="save()" class="px-3 py-1 rounded bg-gray-300 text-gray-600">💾 Save</button>
    </div>
  </div>

  <!-- Layout: Drawer + Content -->
  <div class="flex h-[calc(100vh-56px)]">
    <!-- Left Drawer -->
    <div id="drawer" class="w-64 bg-white border-r border-gray-200 p-4 overflow-y-auto transition-transform transform -translate-x-full fixed sm:relative sm:translate-x-0 z-40 sm:z-auto h-full sm:h-auto">
      <div class="flex justify-between items-center mb-4">
        <h2 class="font-semibold text-gray-700">Files</h2>
        <button id="collapseDrawer" class="text-xl text-gray-500 hover:text-black sm:hidden">⯈</button>
      </div>
      <div id="sheetButtons" class="flex flex-col gap-2"></div>
    </div>

    <!-- Main Content -->
    <div class="flex-1 overflow-auto p-4">
      <div id="emptyState" class="text-center text-gray-500 mt-20">Select a sheet to view data.</div>

      <div class="overflow-auto border rounded shadow bg-white hidden" id="dataTable">
        <table class="min-w-full table-fixed border-collapse">
          <thead>
            <tr id="headerRow"></tr>
          </thead>
          <tbody id="tableBody" class="text-sm"></tbody>
        </table>
      </div>
    </div>
  </div>

  <script src="app.js"></script>
  <script>
    // Drawer toggle behavior
    const drawer = document.getElementById("drawer");
    const toggleDrawerBtn = document.getElementById("toggleDrawer");
    const collapseDrawerBtn = document.getElementById("collapseDrawer");

    toggleDrawerBtn.onclick = () => {
      drawer.classList.toggle("-translate-x-full");
    };

    collapseDrawerBtn.onclick = () => {
      drawer.classList.add("-translate-x-full");
    };

    // Auto-close drawer on sheet select (hooked from JS later)
    function closeDrawer() {
      if (window.innerWidth < 640) drawer.classList.add("-translate-x-full");
    }
  </script>
</body>
</html>
