th {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 8px;
  padding-right: 8px;
  white-space: nowrap;
  position: sticky;
  top: 0;
  z-index: 2;
  background-color: #eee;
}

/* Eye icon style */
.hide-icon {
  opacity: 0;
  transition: opacity 0.2s;
  cursor: pointer;
  font-size: 14px;
  flex-shrink: 0;
}

/* Show icon only on hover */
th:hover .hide-icon {
  opacity: 1;
}
