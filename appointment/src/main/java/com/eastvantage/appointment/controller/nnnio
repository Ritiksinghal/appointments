const rightControls = document.createElement('div');
rightControls.style.position = 'relative';

rightControls.appendChild(hideBtn);
rightControls.appendChild(colPanel);

sheetTitleBar.appendChild(sheetTitle);
sheetTitleBar.appendChild(rightControls);
contentDiv.appendChild(sheetTitleBar);
